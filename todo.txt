doing:

feat: 顶部导航栏优化

添加 i18n 支持 (顶部导航栏优化)

表的定义+SQL预览 和 初始数据+SQL预览 使用tab进行切换

initial data 不填时的默认值问题（允许选择某列不生成）

Common Config fieldtype 针对不同数据库方言做选择的界面展示优化（不要都挤在最后mysql pgsql两列这里配置），最好能够专门做个 Vue 组件来配置

字段注释能不能提供一个自动生成选项含义的复选框，比如：
注释：是否启用
勾选后，mysql生成：是否启用：0-不启用，1-启用，默认1
pgsql生成：是否启用：FALSE-不启用，TRUE-启用，默认TRUE

我们的从磁盘重新加载，和直接打开逻辑，目前我看是两个函数，他们是否值得合并成一个函数？还是就保持现状？

------

配置的时候字段类型不好设置，最好有一个地方专门统一规范一套顶层类型，顶层类型可以分别映射到各个数据库的类型，这样设计字段的时候，只需要选择顶层类型即可（当然也保留针对某个字段类型的数据库方言覆盖输入框）

------

common prompt:

修复编译报错

检查一下代码是否有冗余或不再使用的逻辑，如果有的话，可以清理掉相关 dead code

根据最新的代码更新仓库 README.md 文件

评估一下当前的项目的目录结构和代码逻辑设计是否合理，是否有性能或者安全问题，是否有存在需要重构的部分

在开始之前，请先尽可能了解项目，如果涉及到你拿不准的点，请先问我，所有疑问都明确后再开始行动
